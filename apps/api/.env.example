# HTTP server port (validated default: 3001)
PORT=3001

# PostgreSQL connection string used by TypeORM
DATABASE_URL=postgresql://brainstack:brainstack@localhost:5432/brainstack

# Secret for signing JWT access tokens (minimum 32 characters)
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# Token lifetime — duration suffix (15m, 7d) or seconds (3600)
JWT_EXPIRY=7d

# Redis connection URI for caching and sessions
REDIS_URL=redis://localhost:6379

# Default cache entry TTL in seconds when set() is called without an explicit ttl
REDIS_DEFAULT_TTL=3600

# Enable Stripe card payments (requires Stripe keys below when true)
PAYMENTS_ENABLED=false

# Stripe secret API key (required only when PAYMENTS_ENABLED=true)
# STRIPE_SECRET_KEY=sk_test_replace_with_your_stripe_secret_key

# Stripe webhook signing secret (required only when PAYMENTS_ENABLED=true)
# STRIPE_WEBHOOK_SECRET=whsec_replace_with_your_webhook_signing_secret

# WhatsApp + mobile wallet (bKash / Nagad) — manual confirm via WhatsApp
# BrainStack team confirms payment after parent sends money + screenshot on WhatsApp
WHATSAPP_NUMBER=8801XXXXXXXXX
BKASH_MERCHANT_NUMBER=01XXXXXXXXX
NAGAD_MERCHANT_NUMBER=01XXXXXXXXX

# Public URL of the Next.js frontend (used for CORS)
FRONTEND_URL=http://localhost:3000

# Outbound SMTP relay for transactional email
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASS=replace_with_smtp_password
SMTP_FROM=BrainStack <noreply@example.com>

# IP rate limits for public endpoints (requests per TTL window, per IP)
THROTTLE_LOGIN_LIMIT=5
THROTTLE_LOGIN_TTL_SECONDS=60
THROTTLE_REGISTER_LIMIT=5
THROTTLE_REGISTER_TTL_SECONDS=60
THROTTLE_REGISTRATION_LEADS_LIMIT=10
THROTTLE_REGISTRATION_LEADS_TTL_SECONDS=60
